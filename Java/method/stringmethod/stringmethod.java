public class stringmethod{
    public static void main(String []args){
        String name = " Yash patel ";

        int length = name.length();
        char letter = name.charAt(0);
        int index = name.indexOf("h");
        int lastindex = name.lastIndexOf("a");

       // name = name.toUpperCase();
         // name = name.toLowerCase();  
        // name = name.trim();
        //name = name.replace("s", "d");
        //System.out.println(name.isEmpty());
        //string.contains() method to check what does string contains
        //string.equals() metod to compare two string and check if they are same
        //To ignore case sensitivity use string.equalsIgnoreCase

    }
}